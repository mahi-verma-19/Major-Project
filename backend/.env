# Server
PORT=5000

# MongoDB connection (replace with your own cluster or local MongoDB)
MONGO_URI=mongodb://127.0.0.1:27017/algoodyseeey

# JWT secret key (for authentication, later use)
JWT_SECRET=yourSuperSecretKey123

# (Optional) If using Judge0 or other API for code execution
# JUDGE0_API_KEY=your_api_key_here